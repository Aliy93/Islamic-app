# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.

## Optional Environment Variables

- `NOMINATIM_CONTACT_EMAIL`: Contact email appended to reverse geocoding requests so the `/api/reverse-geocode` proxy identifies itself to OpenStreetMap Nominatim more reliably in production.
